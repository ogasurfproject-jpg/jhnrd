# 運営と、利益相反の開示

このデータベースは、**誰が作り、誰に養われ、何を決められるのか**を先に書いておきます。
出典の素性を三段階で名乗ることを規律にしている以上、**このデータベース自身の素性を名乗らないという例外は作れません。**

---

## 1. 誰が作っているか

The HORIZONs株式会社（法人番号 7021001075279）
監修 大賀俊勝（[ORCID 0009-0000-9180-903X](https://orcid.org/0009-0000-9180-903X)）

一社が単独で維持しています。**査読機関でも、公的機関でも、業界団体でもありません。**
厚生労働省・都道府県・保険者・その他の行政機関とは、いかなる関係もありません。ここに載っている内容について、**行政の確認は一切受けていません。**

---

## 2. 利益相反の開示

**The HORIZONs株式会社は、このデータベースを裏付けに使った有償のサービスを、訪問看護事業所に販売しています。**
対価は初期構築費と月額の形で受け取っています。つまり、**このデータベースを作っている者は、このデータベースが役に立つと思われることで収入を得ます。**

### 何が歪みうるか（こちらから先に書きます）

| 歪む方向 | 具体的に何が起きうるか |
| --- | --- |
| 網羅しているように見せたくなる | 項目数を大きく見せる。未確認を空欄にして数に入れない |
| 危険を大きく見せたくなる | 「減算の危険がある」と読める書き方をして、サービスを必要に見せる |
| 都合の悪い食い違いを消したくなる | 出典が割れたとき、商品説明と揃う側だけを残す |
| 直したことを隠したくなる | 過去の誤りを黙って書き換える |

### それに対して、何を先に縛ってあるか

- **項目数を README の先頭に置き、「網羅した数字ではない」と明記しています。**（いま 33 項目）
- **未確認の要件の件数を公開しています。**（いま 48 件）空欄にはしません。
- **食い違いは両方残します。**（`conflicts`。全件と未解決件数を別に数えます）
- **取りに行って取れなかった記録（`attempts`）も公開しています。** 出典一覧には取れたものしか載らないためです。
- **訂正は元の記述を消しません。** `correction` として上に積みます。過去の判断を後から検証できなくなるためです。
- **README に書いてある数字は `status.json` から機械生成しています。** 手で書き換えると CI が落ちます（`tools/update_readme.py --check`）。**都合よく数字を盛ることが、技術的にできません。**
- **版ごとに tag が打ってあります。** どの日にどう書いてあったかを、後から誰でも取り出せます。

### 有償サービス側との関係

有償サービスが参照しているのは、**このリポジトリの内容の写し**です。
**写しがこのリポジトリより古いと、あちら側の CI が赤で止まります**（版番号を突き合わせています）。
**有償の顧客だけが受け取る、別版・拡張版のデータはありません。** ここにあるものが全部です。

### 資金と、受け取っていないもの

- 収入源は**顧客からの対価のみ**です。
- 国・自治体・保険者・業界団体・医療機器/システムベンダ・製薬企業からの**資金提供、助成、スポンサー、広告は受けていません。**
- このデータベースに**掲載料はありません。** 載ること・載らないことに対して、誰からも対価を受け取っていません。

### 事業所から聞いた話の扱い

導入先の事業所から聞いた現場の運用（`field_reports`）は、**同意なしにこのデータベースへ入れません。**
いま `field_reports` は **0 件**です。そして `field_reports` は、たとえ入っても**規則の出典ではありません**（`field_reports_note` に明記）。

---

## 3. 何を決められて、何を決められないか

**決められること**

- ある要件を、どの素性（`statute` / `agency` / `secondary`）の出典に紐づけるか
- ある記述を `confirmed: false` にするか
- 食い違いを「解決」と書くか、開いたまま残すか
- 版を切るか

**決められないこと**

- **算定できるかどうか。** 判定はしません。検査器（`tools/validate.py`）が、断定と読める文をデータの中に置くことを拒否します。
- **原文に書いていないことを、書いてあることにすること。**

このデータベースが言えるのは「**この減算には A・B・C の要件があり、C はまだ原文で確認できていない**」までです。
線を引いている理由は単純です。**返戻になったとき、責任の所在が壊れるからです。**

---

## 4. 訂正はどう処理されるか

1. Issue で指摘を受け取ります（事業所の運営者である必要はありません）。
2. 指摘の根拠の素性を見ます。**`statute`（原文）は `agency` に、`agency` は `secondary` に優先します。**
3. 直すときは、**元の記述を消さず** `correction` として積みます。何がどう間違っていたかが読めるようにします。
4. 出典どうしが割れたままなら、**解決したことにせず** `conflicts` に両方を残します。
5. 版（seed 番号）を上げ、tag を打ちます。

**訂正は不具合として扱います。** 直したことは、隠す対象ではなく、記録する対象です。

---

## 5. 版と引用

- 版は `2024-kaitei.seed.N` の形です。**版が違えば中身が違います。**
- 引用するときは**版を添えてください**（`CITATION.cff` 参照）。訂正しても元を消さない方針のため、版が分からないと後から突き合わせられません。
- 各版に annotated tag が打ってあります: `git tag -l 'seed.*'`

### DOI（まだ取っていません）

`.zenodo.json` は用意してあります（中身は `status.json` から生成）。取るなら手順は次のとおりです。

1. Zenodo で GitHub 連携を開き、`ogasurfproject-jpg/jhnrd` を on にする
2. GitHub で Release を切る（既存の `seed.N` tag から作れます）
3. Zenodo が DOI を発行する。**版ごとの DOI と、全版をまとめる concept DOI の二つが出ます**
4. `CITATION.cff` に `doi:` を足し、README にバッジを足す

**取るまで、README に DOI があるかのように書きません。** 無いものを、あるように見せない。

---

## 6. 連絡先

`contact@the-horizons-innovation.com`

**この文書自体の誤り（利益相反の書き漏らしを含む）も、Issue で指摘してください。**
